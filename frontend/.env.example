VITE_API_URL=http://localhost:5000
# Milestone 4
VITE_TURNSTILE_SITE_KEY=
# Milestone 8: client-side token (safe to expose) for Paddle.js overlay checkout.
# Must match the sandbox/production state of the backend's PADDLE_SANDBOX.
VITE_PADDLE_CLIENT_TOKEN=
VITE_PADDLE_ENVIRONMENT=sandbox
